# 贡献指南

感谢你愿意为 `public-figure-research` 贡献！请阅读以下指南，确保你的贡献符合项目规范。

## 行为准则

- 尊重所有贡献者，保持专业、友善的沟通
- 本项目的核心是**合规、安全、稳定**的公众人物调研，任何贡献不得削弱这些原则

## 如何贡献

### 1. 报告问题

- 使用 GitHub Issues 报告 bug 或功能建议
- 请包含：复现步骤、期望行为、实际行为、环境信息（OS/Python 版本）
- **不要**在 issue 中提交任何 cookie、token、密钥等敏感信息

### 2. 提交代码

- Fork 本仓库，创建功能分支（`feature/xxx` 或 `fix/xxx`）
- 遵循现有代码风格（Python 3.9+ 兼容，含 `# -*- coding: utf-8 -*-` 声明）
- 提交信息使用清晰的中文或英文描述
- 提交前运行语法检查：`python3 -m py_compile scripts/*.py`
- 通过 Pull Request 提交，描述改动内容和测试结果

### 3. 代码规范

- **不硬编码任何凭证**（cookie、token、API key）
- **不提交任何敏感文件**（见 `.gitignore`）
- **不硬编码绝对路径**——脚本需自定位（`Path(__file__)` / `dirname $0`），依赖目录用环境变量或 config 注入
- **脱敏要求**——任何 1Password 引用用 `op://保险库/条目/字段` 占位符，密钥文件路径用环境变量可配置
- **零弹窗优先**——cookie 提取默认用 peanuts/CDP（零弹窗），Keychain 仅作兜底且不自动弹窗
- 新增脚本需包含：模块 docstring、参数校验、错误处理
- 保持与现有脚本一致的接口风格

## 安全要求（强制）

本项目涉及浏览器 cookie 提取和平台数据访问，**任何贡献必须遵守**：

1. **绝不提交真实 cookie/token/密钥**到仓库
2. **绝不绕过用户同意门禁**——cookie 提取前必须征得用户明示同意
3. **绝不削弱安全护栏**——临时存储、过期、用后即删等机制不可移除
4. **绝不鼓励滥用**——不得添加用于高频抓取、批量封号、绕过风控的"加速"功能
5. **绝不自动弹窗**——cookie 解密默认零弹窗，Keychain 仅在已授权时静默工作
6. 新增平台支持时，必须同步更新隐私声明和平台风险说明

## 测试要求

- 提交前必须本地测试通过
- 涉及平台访问的改动，需说明测试结果（成功/失败/风控情况）
- 抖音等风控严格的平台，测试需遵守频率限制（间隔 8-10 秒）
- cookie 解密测试须从真实浏览器全新获取，不用缓存

## 版本管理

- 遵循 [语义化版本](https://semver.org/lang/zh-CN/)（SemVer）
- 版本号格式：`vX.Y.Z`
- 每次发布需更新 `CHANGELOG.md`

## 协议

贡献即表示你同意你的代码以 [MIT](LICENSE) 协议发布。
