# 安全与合规声明

> ⚠️ **请在使用本 skill 前仔细阅读本文件。** 本 skill 涉及浏览器 cookie 提取和第三方平台数据访问，存在安全与合规风险。使用即表示你理解并接受以下声明。

## 一、敏感能力声明

本 skill 包含以下**敏感能力**，请知悉：

### 1. 浏览器 Cookie 提取

- **能力**：`scripts/fetch_cookie.py` 用三级策略解密本地浏览器 cookie 数据库
- **范围**：仅提取目标平台（微博/抖音/小红书/B站）的最小 cookie 集合
- **风险**：cookie 是登录凭证，若被泄露可导致账号被接管
- **解密策略（零弹窗优先）**：
  - 第1级 peanuts 纯数据解密：手写 AES 解密，**不触发系统弹窗**
  - 第2级 CDP 拿明文：连接已运行浏览器，**不触发系统弹窗**
  - 第3级 Keychain 兜底：用 rookiepy 读 Keychain，**仅在已授权时静默工作**，绝不自动弹窗
- **护栏**：
  - 每次提取前必须征得用户**明示同意**
  - 仅存临时目录 `$TMPDIR/public-figure-research/`，权限 `0600`
  - 有效期 ≤1 小时，任务结束自动移入 `~/.Trash`
  - 绝不上传、不打印、不进日志、不写入配置文件
  - 安全 SQL：host_key + name 双条件过滤，防伪装域名；WAL 友好只读
  - Keychain 兜底带超时，防循环卡死

### 2. 平台数据访问

- **能力**：`scripts/query_platform.py` 通过纯 Python 签名（小红书）或 playwright 无头 chromium（抖音）访问平台数据
- **范围**：仅查询公开的粉丝量数据
- **风险**：可能触发平台风控（verify_check），导致 IP 或账号被限制

## 二、平台风险说明

### 抖音（高风险）

- **verify_check 风控**：抖音对非真实浏览器指纹或数据中心 IP 有严格风控，连续搜索会触发验证码拦截
- **频率限制**：每次查询间隔需 8-10 秒，频繁访问可能导致 IP 或 cookie 被封
- **账号风险**：高频自动化操作可能触发滑块验证码、禁言，严重时封号
- **缓解**：使用 playwright 无头 chromium（独立环境）+ 严格频率控制 + 风控检测后立即停止

### 小红书（中风险）

- **签名风控**：小红书使用 x-s/x-s-common 签名，算法更新频繁，可能失效
- **IP 限制**：小红书对数据中心 IP 有风控，可能返回空结果
- **缓解**：使用纯 Python 签名（xhshow）+ 真实 cookie，降低触发概率

### 微博 / B站（低风险）

- 使用官方/成熟库（crawl4weibo / bilibili-api），风控风险较低
- 仍建议控制访问频率

## 三、法律与合规声明

> ⚠️ **重要法律提示**

1. **平台服务条款**：本 skill 的自动化数据访问可能违反微博、抖音、小红书、B站等平台的服务条款。**使用者需自行承担由此产生的风险**，包括但不限于账号限制、封禁、法律追责。

2. **数据使用**：本 skill 仅用于**个人学习、研究、正当商业调研**。禁止用于：
   - 大规模数据抓取、转售、牟利
   - 侵犯他人隐私、肖像权、名誉权
   - 绕过平台安全机制进行恶意操作

3. **Cookie 安全**：cookie 属于敏感凭证。使用者必须：
   - 妥善保管提取的 cookie，不得泄露给第三方
   - 使用后立即清理（本 skill 已自动处理）
   - 不得将 cookie 提交到任何公开渠道

4. **免责声明**：本项目作者不对使用者因使用本 skill 而产生的任何直接或间接损失负责，包括但不限于账号封禁、数据丢失、法律纠纷。

## 四、使用者责任

使用本 skill 即表示你同意：

1. 你已阅读并理解本声明
2. 你仅将本 skill 用于合法、正当的目的
3. 你自行承担平台风控、账号风险、法律风险
4. 你不会将本 skill 用于任何恶意或非法用途

---

**如有疑问，请在使用前联系项目维护者。**
