[project]
name = "social-follower-xiaohongshu"
version = "2.0.0"
description = "小红书用户粉丝数据查询工具"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.28.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"
